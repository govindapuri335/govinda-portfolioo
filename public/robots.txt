# Allow all crawlers
User-agent: *
Allow: /

# Sitemap location
# Update: point sitemap to the site URL configured in `config/site.ts` (change if you deploy to a custom domain)
Sitemap: https://github.com/Krishna8665/sitemap.xml